// Registers @hbeneke/parley native web components (e.g. <site-footer>).
// Client-only: the package touches `document` at import time.
import '@hbeneke/parley'

export default defineNuxtPlugin(() => {})
